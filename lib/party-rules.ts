export const MAX_PENDING_TRACKS_PER_PERSON = 100;
export const MAX_PARTICIPANTS_PER_ROOM = 100;
/** Reactions are refused this long after a song starts, so boos aimed at the previous song cannot land on the new one. */
export const REACTION_GRACE_MS = 3_000;
